#include "inputpro.h"
#include <stdio.h>
#include <stdlib.h>
#define stackmax 100000

void  main () {
	shadowstack * s = (shadowstack *)malloc(stackmax*sizeof(shadowstack));
	count = 100;
	printf("The value of count is : %d\n", count);
}
